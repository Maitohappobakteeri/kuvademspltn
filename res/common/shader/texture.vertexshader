attribute highp vec3 modelVertex;
attribute highp vec2 textureUvVec;

uniform highp mat4 mvpMatrix;

varying highp vec2 uvVec;

void main()
{
    gl_Position.xyzw = mvpMatrix * vec4(modelVertex,1.0);
    uvVec = textureUvVec;
}
