varying highp vec3 normal;

uniform highp vec4 inColor;

void main()
{
    highp vec3 lightSource = normalize(vec3(1.0,1.0,1.0));
    gl_FragColor.rgb = inColor.rgb * (0.6666666 + 0.333333 * dot(normal, lightSource));
    gl_FragColor.a = inColor.a;
}
